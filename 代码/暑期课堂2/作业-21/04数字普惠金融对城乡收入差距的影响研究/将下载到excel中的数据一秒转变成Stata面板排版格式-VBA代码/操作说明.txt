操作步骤
1 将保存在txt文件中的代码复制到excel模块代码窗口
2 用鼠标选定表格中数据区域 
3 返回代码窗口执行代码



其中有几个需要地方的地方：

1 在运行代码前一定要确保已经将需要转化的单元格区域选定，
  因为代码中的某些变量是根据选取的单元格来赋值的，如果遗漏将会报错。

2 执行之后会新建一个以“整理后”命名的表，用来保存整理后的数据，
  如果有多个表格数据要处理，请在每次运行代码完后，将生成的表格手动重新命名，
  否则执行代码时表的名称会冲突，导致代码执行出错。




操作详见操作示例